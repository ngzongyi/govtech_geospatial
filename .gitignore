# Raw source data — not committed (the CEA CSV alone is ~123 MB, over GitHub's
# 100 MB per-file push limit). See section-1-question-1/codes/README.md for how to
# obtain/place it before running the notebook.
section-1-question-1/codes/data/

# Regenerated by running the notebook — derived from data/ + the notebook's own
# code, and the charts are also embedded inline in the notebook's saved output,
# so nothing here is lost by excluding it.
section-1-question-1/codes/output/
section-1-question-2/codes/output/

# Python / Jupyter housekeeping
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.env

# OS housekeeping
.DS_Store
Thumbs.db
